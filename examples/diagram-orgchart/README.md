# Diagram Org Chart Example

Beispiel für ein Organigramm mit `hierarchical`-Layout.

Kompilieren:

```bash
cargo run -p docgen -- compile examples/diagram-orgchart/diagram.json -o examples/diagram-orgchart/output/diagram.pdf
```
