# Diagram Roadmap Example

Beispiel für ein Portfolio-Roadmap-Layout mit Phasen und Streams.

Kompilieren:

```bash
cargo run -p docgen -- compile examples/diagram-roadmap/diagram.json -o examples/diagram-roadmap/output/diagram.pdf
```
