# Diagram Swimlane Example

Beispiel für ein Prozessdiagramm mit dem integrierten `swimlane`-Layout.

Kompilieren:

```bash
cargo run -p docgen -- compile examples/diagram-swimlane/diagram.json -o examples/diagram-swimlane/output/diagram.pdf
```
