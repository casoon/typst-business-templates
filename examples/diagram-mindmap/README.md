# Diagram Mindmap Example

Beispiel für eine Mindmap mit `radial`-Layout.

Kompilieren:

```bash
cargo run -p docgen -- compile examples/diagram-mindmap/diagram.json -o examples/diagram-mindmap/output/diagram.pdf
```
