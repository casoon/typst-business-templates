# Diagram Timeline Example

Beispiel für eine einfache Timeline mit dem integrierten `timeline`-Layout.

Kompilieren:

```bash
cargo run -p docgen -- compile examples/diagram-timeline/diagram.json -o examples/diagram-timeline/output/diagram.pdf
```
