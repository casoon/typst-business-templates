# Diagram Quadrant Example

Beispiel für eine Priorisierungsmatrix mit dem integrierten `quadrant`-Layout.

Kompilieren:

```bash
cargo run -p docgen -- compile examples/diagram-quadrant/diagram.json -o examples/diagram-quadrant/output/diagram.pdf
```
