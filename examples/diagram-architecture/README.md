# Diagram Architecture Example

Beispiel für ein Software-/Systemdiagramm mit `layered`-Layout.

Kompilieren:

```bash
cargo run -p docgen -- compile examples/diagram-architecture/diagram.json -o examples/diagram-architecture/output/diagram.pdf
```
