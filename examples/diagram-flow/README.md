# Diagram Flow Example

Beispiel für einen Prozess- und Freigabe-Workflow mit `layered`-Layout.

Kompilieren:

```bash
cargo run -p docgen -- compile examples/diagram-flow/diagram.json -o examples/diagram-flow/output/diagram.pdf
```
